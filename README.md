# README

- raw files from master branch in folder `schema` can be used with: <https://purl.org/dat/schema/PATH>
- for new schames, there should be a conversion function, so we can automatically create an update chain
